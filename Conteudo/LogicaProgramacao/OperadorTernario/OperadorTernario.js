const pontuacao = 1001;
const nivelUsuario = pontuacao >= 1000 ? 'Usuario vip' : 'Usuario não vip';
// (condicao) ? (Valor true) : (Valor false);

console.log(nivelUsuario);

// if (pontuacao >= 1000) {
//     console.log('Usuario vip');
// }else {
//     console.log('Usuario não vip');
// }